<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title>Ro'yxatdan o'tish</title>
    <style>
      html,
      body {
        height: 100%;
      }

      body {
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
      }

      .form-container {
        width: 100%;
        max-width: 400px;
        padding: 30px;
        background: #ffffff;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .form-floating:focus-within {
        z-index: 2;
      }

      .bottom-links {
        margin-top: 15px;
        font-size: 14px;
        text-align: center;
      }

      .bottom-links a {
        margin: 0 5px;
      }
    </style>
  </head>
  <body>
    <main class="form-container">
      <form action="/users/register" method="post">
        <h1 class="h3 mb-3 fw-normal text-center">Ro'yxatdan o'tish</h1>

        <% if (error) { %>
        <div class="alert alert-danger text-center" role="alert">
          <%= error %>
        </div>
        <% } %>

        <div class="form-floating mb-2">
          <input
            type="text"
            name="name"
            class="form-control"
            id="floatingName"
            placeholder="Ism"
            required
          />
          <label for="floatingName">Ism</label>
        </div>

        <div class="form-floating mb-2">
          <input
            type="text"
            name="phoneNumber"
            class="form-control"
            id="floatingPhone"
            placeholder="Telefon raqam"
            required
          />
          <label for="floatingPhone">Telefon raqam</label>
        </div>

        <div class="form-floating mb-2">
          <input
            type="email"
            name="email"
            class="form-control"
            id="floatingEmail"
            placeholder="Email"
            required
          />
          <label for="floatingEmail">Email</label>
        </div>

        <div class="form-floating mb-2">
          <input
            type="password"
            name="password"
            class="form-control"
            id="floatingPassword"
            placeholder="Parol"
            required
          />
          <label for="floatingPassword">Parol</label>
        </div>

        <div class="form-floating mb-2">
          <input
            type="date"
            name="birthDate"
            class="form-control"
            id="floatingBirth"
            placeholder="Tug'ilgan sana"
          />
          <label for="floatingBirth">Tug'ilgan sana</label>
        </div>

        <div class="mb-3">
          <label class="form-label d-block">Jinsi</label>
          <div class="form-check form-check-inline">
            <input
              class="form-check-input"
              type="radio"
              name="gender"
              id="male"
              value="male"
              required
            />
            <label class="form-check-label" for="male">Erkak</label>
          </div>
          <div class="form-check form-check-inline">
            <input
              class="form-check-input"
              type="radio"
              name="gender"
              id="female"
              value="female"
            />
            <label class="form-check-label" for="female">Ayol</label>
          </div>
        </div>

        <button class="w-100 btn btn-lg btn-primary" type="submit">
          Ro'yxatdan o'tish
        </button>

        <div class="bottom-links">
          <p>
            Akkountingiz bormi?
            <a href="/users/login">Kirish</a>
          </p>
        </div>
      </form>
    </main>
  </body>
</html>
