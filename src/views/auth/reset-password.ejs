<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title>Parolni tiklash</title>
    <style>
      html,
      body {
        height: 100%;
      }

      body {
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
      }

      .form-container {
        width: 100%;
        max-width: 400px;
        padding: 30px;
        background: #ffffff;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      }

      .form-floating:focus-within {
        z-index: 2;
      }

      .bottom-links {
        margin-top: 15px;
        font-size: 14px;
        text-align: center;
      }

      .bottom-links a {
        margin: 0 5px;
      }
    </style>
  </head>
  <body>
    <main class="form-container">
      <form action="/users/reset-password?token=<%= token %>" method="post">
        <h1 class="h3 mb-3 fw-normal text-center">Parolni tiklash</h1>

        <% if (error) { %>
        <div class="alert alert-danger text-center"><%= error %></div>
        <% } %> <% if (message) { %>
        <div class="alert alert-success text-center"><%= message %></div>
        <% } %>

        <div class="form-floating mb-3">
          <input
            type="password"
            name="password"
            class="form-control"
            id="floatingPassword"
            placeholder="Yangi parol"
            required
          />
          <label for="floatingPassword">Yangi parol</label>
        </div>

        <button class="w-100 btn btn-lg btn-primary" type="submit">
          Tiklash
        </button>
      </form>
    </main>
  </body>
</html>
