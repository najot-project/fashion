<!DOCTYPE html>
<html lang="uz">
<head>
  <meta charset="UTF-8">
  <title>Buyurtma tafsilotlari</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8">

        <div class="card shadow-sm rounded-4">
          <div class="card-body">
            <% if (order) { %> <!-- Check if order exists -->
              <h3 class="card-title text-center text-primary mb-4">
                🧾 Buyurtma ID: <%= order._id %>
              </h3>

              <ul class="list-group mb-4">
                <% order.items.forEach(item => { %>
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    👗 <strong><%= item.product.name %></strong>
                    <span class="badge bg-secondary rounded-pill">Miqdor: <%= item.quantity %></span>
                  </li>
                <% }) %>
              </ul>
            <% } else { %> <!-- If no order exists -->
              <h3 class="text-center text-danger">Buyurtma topilmadi</h3>
            <% } %>

            <div class="text-center">
              <a href="/orders" class="btn btn-outline-primary">🔙 Buyurtmalar ro‘yxatiga qaytish</a>
            </div>

          </div>
        </div>

      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
